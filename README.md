# 📦 Загрузчик файлов

Данный проект представляет собой сервис для асинхронной загрузки файлов с гарантией идемпотентности, консистентности данных и корректной обработки ошибок.
Приложение реализовано на **Spring Boot (WebFlux)** и использует **MongoDB** в качестве базы данных.

## 🛠 Функционал

1. Сервис принимает файл в формате multipart/form-data 
2. После получения файла запускается асинхронная обработка, в рамках которой 
файл передаётся во внешнее файловое хранилище 
3. Ожидается результат операции загрузки 
4. После успешного завершения загрузки, гарантированно создаётся запись в базе 
данных с информацией о файле

## 📃 Требования

* Решение должно учитывать возможность повторных запросов и не приводить к 
созданию дублирующих записей или повторной обработке одного и того же 
документа (Либо для одного и того же клиента, тут про Идемпотентность) 
* Обработка не должна блокировать HTTP-запрос пользователя на время 
выполнения операций с файловым хранилищем 
* В процессе выполнения необходимо корректно обрабатывать ошибки, не допуская 
неконсистентного состояния системы, при котором файл загружен без записи в 
базе или запись существует без фактически сохранённого файла 
* Все временные ресурсы, используемые при обработке запроса, должны корректно 
освобождаться независимо от результата выполнения.

---

## 🚀 Требования для запуска

Для запуска проекта на локальной машине необходимо установить:

* **Docker** (версия 20+)
* **Docker Compose** (обычно уже входит в Docker Desktop)
  
---

## ▶️ Инструкция по запуску проекта

### 1️⃣ Клонировать репозиторий

```bash
git clone [<URL_РЕПОЗИТОРИЯ>](https://github.com/ozelyaaa/FileUploader)
cd <ИМЯ_ПРОЕКТА>
```

---

### 2️⃣ Запустить приложение с помощью Docker Compose

В корневой директории проекта выполнить:

```bash
docker compose up -d --build
```

### 3️⃣ Проверить, что приложение запущено

После успешного запуска:

* Приложение доступно по адресу:
  **[http://localhost:8080](http://localhost:8080)**
* MongoDB запущена внутри Docker-контейнера

---

## 🔐 Тестирование API

### Регистрация пользователя

```bash
curl -X POST http://localhost:8080/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"password123"}'
```

---

### Авторизация (получение JWT токена)

```bash
curl -X POST http://localhost:8080/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"password123"}'
```

В ответе будет возвращён JWT токен:

```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

---

### Загрузка файла

```bash
curl -X POST http://localhost:8080/files \
  -H "Authorization: Bearer <JWT_ТОКЕН>" \
  -F "file=@example.txt"
```

После этого файл будет:

* асинхронно обработан
* сохранён внутри контейнера
* зарегистрирован в базе данных

Повторная загрузка того же файла от того же пользователя **не приведёт к созданию дубликата**.

---

:tada:

*KaspiLab, 2026*
